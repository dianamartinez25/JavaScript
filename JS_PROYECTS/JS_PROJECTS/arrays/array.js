// Declaración de Arrays.
/*const numeros = [1, 2, 3, 4, 5];
const numeros2 =  new Array(); // const numeros = [];
console.log (numeros[1]); //2

// Añadir elementos.
//se pueden añadir elementos  al principio o al final
//Añadir al final:
numeros.push(6);
//Añadir al principio:
numeros.unshift(0);
// anadir elementos, en este caso, desde la posicion 0 , no me borra ninguna = 0, añadir 5 y 5 
numeros.splice(0,0,5,5)
//BORRAR.  pop() o shift(), delete()
//shift(), borra el primer elemento.
numeros.shift;
//pop(), borra el último elemento.
numeros.pop();
// delete() borra la posición en concreto.
delete numeros [4];
// splice() quitar una posicion en cocreto en este caso desde la posicion 0 quitar la posicion 1.
numeros.splice(0, 1)

// Crea un nuevo array con los elementos que le decimos. desde la posicion 0 coge 2 elementos.
numeros.slice(0, 2)
document.write(numeros);

// ORDENAR. 
const num = [25, 103, 13]
const ordenado = num.sort()
// Ordenar de forma ascendente
num.sort(function (a, b) {return a - b})
// Ordenar de forma descendente
num.sort(function (a, b) {return b - a})*/


// MAP() (n) es cada uno de los elementos del array
const numeros = [1, 5, 10]
/*numeros.map(function(n){return n*2})
//otra forma de map
numeros.map(n =>n*2)*/

// FILTER
/*numeros.filter(function(n) {return n>3})*/

//console.log(numeros.every(function(n) {return n>3}))

/*//FIND, busca solamente el primer elemento
numeros.find(function(n) {return n>3});*/

//EVERY
/*let a = numeros.every(function(n) {return n>3})
document.write(a)*/

//SOME
/*let a = numeros.some(function(n) {return n>3});
document.write(a)*/

//REDUCE() da como resultado un único valor 

document.write(a)