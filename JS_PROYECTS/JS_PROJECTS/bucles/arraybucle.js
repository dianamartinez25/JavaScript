// 05 Arrays
// recorrer cada una de las posicones del array
const numeros = [1, 2, 3, 4, 5];
for (let i = 0; i<numeros.length; i++) {
    console.log(numeros[i])}

//
for (let e of numeros) {
    console.lof(e)}

// for Each, por cada elemento
numeros.forEach (function (n) {
    console.log (n)})

